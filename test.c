int x, y, z, c;         
float pi = 3.14;
char letter = 'a';

if (x < 10) {
    y = x * 2;
} else {
    y = x / 2;
}

int num;
printf("Ingrese un número entero: ");
scanf("%d", &num);
printf("El número ingresado es: %d\n", num);

// Comentario de una linea
z = y - 3; 
 